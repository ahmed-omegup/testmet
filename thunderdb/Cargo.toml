[package]
name = "thunderdb"
version = "0.1.0"
edition = "2021"

[dependencies]
futures = "0.3"
tokio = { version = "1", features = ["rt", "macros", "sync"] }
dashmap = "5"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros", "sync"] }
